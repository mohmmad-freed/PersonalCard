// ignore_for_file: file_names

import 'package:flutter/material.dart';

class XsBox extends StatelessWidget {
  const XsBox({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 5,width: 5,);
  }
}
class SBox extends StatelessWidget {
  const SBox({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 10,width: 10,);
  }
}
class XMbox extends StatelessWidget {
  const XMbox({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 15,width: 15,);
  }
}
class MBox extends StatelessWidget {
  const MBox({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 25,width: 25,);
  }
}